server:
  port: 8081
  mode: release

database:
  driver: mysql
  host: ${DB_HOST}
  port: ${DB_PORT}
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
  dbname: ${DB_NAME}

log:
  level: info
  format: json
